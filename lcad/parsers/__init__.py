"""
This module contains all the available parsers.
Parsers may read (load) and write (dump) their respective format.

All parsers are dynamically called by the parserlib module.

Each parser is a file that contains at least either the `load()` or `dump()` functions.
"""
